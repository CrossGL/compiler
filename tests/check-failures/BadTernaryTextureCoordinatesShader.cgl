shader BadTernaryTextureCoordinatesShader {
  compute {
    uniform sampler2D colorMap;
    void main() {
      vec4 color = texture(colorMap, true ? vec3(0.5, 0.5, 0.0) : vec3(0.25, 0.75, 1.0));
      return;
    }
  }
}
