shader BadStorageImageFormatLayoutShader {
  compute {
    layout(local_size_x = 1, local_size_y = 1, local_size_z = 1) in;
    layout(set = 0, binding = 0, format = rgba32i) uniform image2D wrongFormat;
    layout(set = 0, binding = 1, format = rgba32f) uniform sampler2D sampled;

    void main() {
      return;
    }
  }
}
