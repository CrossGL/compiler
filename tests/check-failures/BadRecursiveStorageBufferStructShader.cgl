shader BadRecursiveStorageBufferStructShader {
  struct Node {
    Node next;
    float mass;
  }

  compute {
    layout(set = 0, binding = 0) buffer Node* payloads;

    void main() {
      return;
    }
  }
}
