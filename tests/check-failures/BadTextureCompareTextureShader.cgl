shader BadTextureCompareTextureShader {
  compute {
    uniform sampler2D colorMap;
    sampler linearSampler;
    void main() {
      float visibility =
          textureCompare(colorMap, linearSampler, vec2(0.5, 0.5), 0.25);
      return;
    }
  }
}
