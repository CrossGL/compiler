shader BadStorageImageValueUseShader {
  compute {
    layout(set = 0, binding = 0) uniform image2D colorImage;

    void main() {
      ivec2 pixel = ivec2(0, 1);
      vec4 color = vec4(0.0, 0.25, 0.5, 1.0);
      vec4 stored = imageStore(colorImage, pixel, color);
      return;
    }
  }
}
