shader BadIncrementDecrementExpressionShader {
  compute {
    layout(local_size_x = 1, local_size_y = 1, local_size_z = 1) in;

    void main() {
      int index = 0;
      int next = index++;
      return;
    }
  }
}
