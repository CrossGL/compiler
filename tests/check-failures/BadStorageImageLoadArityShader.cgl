shader BadStorageImageLoadArityShader {
  compute {
    layout(set = 0, binding = 0) uniform image2D colorImage;

    void main() {
      vec4 color = imageLoad(colorImage);
      return;
    }
  }
}
