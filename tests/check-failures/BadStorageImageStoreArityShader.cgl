shader BadStorageImageStoreArityShader {
  compute {
    layout(set = 0, binding = 0) uniform image2D colorImage;

    void main() {
      ivec2 pixel = ivec2(0, 0);
      vec4 color = vec4(1.0, 0.0, 0.0, 1.0);
      imageStore(colorImage, pixel);
      return;
    }
  }
}
