shader WhileComputeShader {
  compute {
    layout(local_size_x = 1, local_size_y = 1, local_size_z = 1) in;
    layout(set = 0, binding = 0) buffer float* values;

    void main() {
      int i = 0;
      while (i < 4) {
        values[i] = values[i] + 1.0;
        i++;
      }
      return;
    }
  }
}
