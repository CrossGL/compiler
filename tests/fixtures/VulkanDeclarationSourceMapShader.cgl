shader VulkanDeclarationSourceMapShader {
  compute {
    layout(local_size_x = 1, local_size_y = 1, local_size_z = 1) in;
    layout(set = 0, binding = 0) buffer float* values;

    void main() {
      float untouched;
      float seeded = 1.0;
      untouched = seeded + 1.0;
      values[0] = untouched;
      return;
    }
  }
}
