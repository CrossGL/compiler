shader ResourceLocationAliasShader {
  struct Params {
    float value;
  }

  compute {
    layout(local_size_x = 1, local_size_y = 1, local_size_z = 1) in;
    layout(location = 1, binding = 2) uniform Params materialParams;
    layout(set = 0, binding = 0) buffer float* values;
    void main() {
      return;
    }
  }
}
