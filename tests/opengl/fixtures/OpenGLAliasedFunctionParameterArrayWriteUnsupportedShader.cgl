shader OpenGLAliasedFunctionParameterArrayWriteUnsupportedShader {
  const int COUNT = 2;

  compute {
    layout(local_size_x = 1, local_size_y = 1, local_size_z = 1) in;

    float blendAliased(float left[COUNT], float right[COUNT]) {
      left[0] = 1.0;
      return right[0];
    }

    void main() {
      float weights[COUNT];
      weights[0] = 0.0;
      weights[1] = blendAliased(weights, weights);
      return;
    }
  }
}
